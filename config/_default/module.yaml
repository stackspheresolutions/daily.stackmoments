# Hugo module config — Mainroad is installed as a git submodule, no Hugo module imports needed.
module:
  mounts:
  - source: assets
    target: assets
  - source: layouts
    target: layouts
  - source: static
    target: static
  - source: content
    target: content
